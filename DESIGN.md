# 任务清单（Tasklist）设计文档

## 1. 产品概述

任务清单是一款**纯前端、单机优先**的任务管理工具：

- 无服务器、无登录、无网络请求；数据仅存于本机浏览器（localStorage），不上传任何服务器
- 部署于 GitHub Pages，免费 HTTPS，零运维
- 手机端可通过浏览器「安装应用」添加到桌面（独立窗口运行）
- 备份方式：导出/导入 JSON 文件到设备本地，导入支持合并同步

## 2. 整体架构与技术选型

**Vue 3 + Vite + TypeScript**

- 状态管理：Pinia（多列表、展开状态、外观设置全局化）
- i18n：vue-i18n，内置中/英双语，架构支持扩展更多语言
- 无 UI 组件库，手写组件 + CSS 变量主题（浅色/深色）
- 数据持久化：localStorage 单键 `tasklist:v1`，写入时防抖
- PWA：manifest + 简单 Service Worker（离线缓存），支持「安装到桌面」独立窗口
- 部署：GitHub Pages，`npm run build` 产物直接推送，零运维

### 2.1 数据模型

```ts
// localStorage 键：tasklist:v1
interface TaskData {
  version: 1             // 数据版本号，用于迁移（见 4.4）
  lists: List[]
  settings: { theme: 'light' | 'dark'; fontSize: number; lang: string; showDescription: boolean }
  ui: { sidebarCollapsed: boolean; expandedGroupIds: string[] } // 组合展开状态按 ID 记忆
}

interface List {
  id: string            // nanoid
  name: string
  description: string
  items: (Item | Group)[]
}

interface Item {        // 事项
  id: string
  name: string
  description: string
  date?: string         // yyyy-mm-dd，无日期则无时间
  time?: string         // hh:mm，有 time 必有 date
  done: boolean
  createdAt: number
}

interface Group {       // 组合（可递归嵌套，不限深度）
  id: string
  name: string
  description: string
  date?: string
  time?: string
  expanded: boolean     // 展开状态记忆
  items: (Item | Group)[] // 递归
  // 无 done 字段：完成状态与时间戳均由子项递归计算得出，不存库
}
```

### 2.2 开发计划（阶段顺序）

1. 脚手架：Vite + Vue 3 + TS
2. 数据层：类型定义、Pinia store、localStorage 读写（防抖）
3. 布局：左侧栏 / 右侧栏 / 主区，响应式（窄屏覆盖式）
4. 递归渲染：事项与组合的列表、状态计算、分组与排序
5. 新建 / 编辑 / 删除弹窗（Esc / Enter / 点遮罩）
6. 完成状态切换
7. 拖拽（位移阈值 + 半透明预览 + 无效目标还原）
8. 设置弹窗、i18n、浅/深主题、字号
9. 导入 / 导出（同 ID 覆盖合并）
10. PWA（manifest + SW）
11. 欢迎列表默认数据

## 3. 页面与组件设计

### 3.1 左侧栏

- 主区左上角一侧栏按钮，或者手机端左侧边缘右划，展开左侧栏
- 展开左侧栏后左上角的侧栏按钮改为收起按钮
- 左侧栏展示“列表”列表，“列表”的右侧有“编辑”按钮
- 列表的最下方有一新建列表按钮，与其上侧列表样式相同
- 编辑状态下每个按钮最右侧展示【删除】按钮
- 双击列表名称重命名
- 每个列表都对应主区的一个任务清单
- 左侧栏稍宽
- 页面尺寸较窄时为覆盖形式
- 各个列表的颜色随其任务状态决定，颜色参考下方组合的状态判定

### 3.2 右侧栏

- 右侧栏展示功能按钮，稍窄
- 页面尺寸较窄时（手机端）可放置在底部
- 第一个按钮为【+事项】
- 第二个按钮为【+组合】
- 第三个按钮为【编辑】，切换全局编辑模式（开启后事项/组合右侧显示【编辑】【删除】等按钮）
- 第四个按钮为【设置】，打开设置弹窗（见 3.7）
- （还会添加功能TBD）

### 3.3 主区

- 主区为清单内容
  - （红色小字）已逾期 ----------（至右侧）
  - （灰色小字）日期（（x年）x月x日 / 昨天今天明天后天）
  - （红色）事项……
  - （红色）组合……
  - （蓝色小字）未完成 ----------（至右侧）
  - （同上，颜色要同步）
  - （绿色小字）已完成 ----------（至右侧）
  - （同上，颜色要同步）
- 列表无任何事项时，主区展示空态提示文案（如「暂无事项，点击右侧 + 添加」）

### 3.4 事项

- 每个事项以一个相应颜色的横条圆角方框绘制，框内为内容
- 左侧有一空心圆心（未完成），或填充为绿色+背景色勾（已完成）
- （黑色常规）【事项名称】（灰色常规）【 · 事项描述】（无描述时不展示）
- 最右侧展示日期（+时间，如果有）
- 点击左侧圆或者整个事项都可改变其完成状态
- 已完成事项的文字不加删除线、不变灰，仅靠左侧绿色圆勾区分（保持版面干净）
- （编辑状态下）在日期时间的右侧再展示按钮【编辑】【删除】

### 3.5 组合

- 每个组合和事项一样绘制，颜色较深
- 左侧圆改为三角箭头表示展开或未展开状态
- （黑色加粗）【组合名称】（灰色常规）【 · 组合描述】（无描述时不展示）
- 最右侧展示日期（+时间，如果有） · X/Y 已完成
  - X/Y 计数口径：统计组合内**全部递归子孙事项**（含直接子项与深层嵌套），X 为已完成数，Y 为总数；组合自身不计入
- 点击左侧箭头或整个组合都可改变其展开状态
- 展开后在下部缩进，缩进处绘制灰色竖线
- 组合的框体不变，下方绘制子内容（事项或组合），与根层完全相同，都要绘制完成状态与时间小字
- （编辑状态下）在日期时间的右侧再展示按钮【编辑】【+事项】【+组合】【删除】

补充：日期时间及右侧按钮皆为灰色，删除可在光标移上时变红

### 3.6 新建 / 编辑

- 中央弹窗操作

#### 3.6.1 新建/编辑列表

- 输入框：列表名称
- 输入框：列表描述
- 【按钮】取消【按钮】确认

#### 3.6.2 新建/编辑组合

- 输入框：组合名称
- 输入框：组合描述

#### 3.6.3 新建/编辑事项

- 输入框：事项名称
- 输入框：事项描述
- 【开关】日期，点击后下方展示日期选择
- 【开关】时间（日期要作为前置），点击后下方展示时间选择

### 3.7 设置
- 【开关】显示描述
- 【切换】外观：浅色模式 / 深色模式
- 【拖条】字号大小
- 【下拉框】语言
- 【按钮】导入数据
- 【按钮】导出数据
- 【按钮】关于

## 4. 逻辑与交互

### 4.1 状态与时间戳

- 事项时间戳：
  - 有日期+时间 → `Date(date + 'T' + time)` 对应的时间戳
  - 有日期无时间 → 当日 00:00 的时间戳
  - 无日期 → 正无穷（组内排最后）
- 组合时间戳（递归计算）：
  - 若组合内存在已逾期或未完成的递归事项，则为其中最小时间戳（无时间的仍为正无穷）
  - 若组合内的递归事项全部已完成，则为已完成的递归事项中除去无时间后的最大值
  - 若组合内无递归事项，则为正无穷
- 组合完成状态（递归计算，不可手动标记）：
  - 若组合内存在已逾期的递归事项，则为已逾期
  - 若组合内的递归事项全部已完成，则为已完成
  - 其他情况，则为未完成
- 已逾期判定：未完成且时间戳 < 当前时刻（有日期才可能逾期）

#### 4.2 排序与分组

- 主区分三组，自上而下：已逾期 → 未完成 → 已完成
- 组内按时间戳升序（最早的在上），同一时间戳按名称排序；无时间戳的排组内最后
- 组间以日期分隔小字展示（灰色小字：x月x日 / 昨天今天明天后天）
- 组标题（已逾期/未完成/已完成）颜色与状态色同步（红/蓝/绿）
- 各列表在左侧栏的颜色同样随其状态判定（红色/蓝色/绿色）

#### 4.3 颜色映射

- 已逾期 = 红色；未完成 = 蓝色；已完成 = 绿色
- 组合颜色比同级事项深一档
- 分组小字与状态色同步；日期时间及右侧按钮为灰色，删除按钮在光标移上时变红

#### 4.4 交互

- 删除列表/事项/组合时要弹窗二次确认
- 新建/编辑/删除弹窗支持 Esc 取消、Enter 确认、点击别处取消
- 拖拽：
  - 位移阈值判定（位移 > 8px 进入拖拽，否则视为点击），配合半透明预览
  - 拖动一个事项或组合到另一个组合中，放入其子层
  - 拖动到另一个事项上，自动创建一个组合包含原来两个元素，组合名称取两个名称拼接「A · B」，描述留空
  - 拖动到空白处，回到根层
  - 放开时未落入有效目标则还原原位
- 逾期状态自动刷新：运行期间每 60 秒重算一次状态；页面从后台切回前台（visibilitychange）时立即重算
- 侧栏折叠 / 主题 / 字号 / 语言 / 组合展开状态均持久化记忆
- 导入合并：同 ID 覆盖本地，其余保留；导入前弹窗确认
- 导入校验：导入前校验 JSON 结构与字段合法性，格式错误或字段缺失时弹窗提示并拒绝导入（不破坏本地数据）
- 数据损坏与迁移：读取 localStorage 解析失败或数据版本不符时，先备份损坏数据到 `tasklist:backup`，再重置为默认空数据并提示用户（不白屏）；后续版本变更走 `tasklist:v1` 的版本号迁移
- 跨标签页同步：监听 `storage` 事件，其他标签页写入时本页自动刷新内存数据（不重载页面）
- 欢迎列表：启动时判断左侧栏列表为空则自动创建，默认内容放在 `src/data/welcome.json`（结构即 List，见 2.1），可直接编辑该文件随时更改；其中 `date` 字段除绝对日期 `yyyy-mm-dd` 外，支持相对标记 `today` / `tomorrow` / `yesterday` / `today+N` / `today-N`，首次创建时按当时日期转换为绝对日期（`time` 仍为固定 `hh:mm`）
- 各种名称必填，描述选填