# 任务清单（Tasklist）设计文档

> 本文档是项目的**设计画像**，保存项目的一切设计细节，以陈述语气书写、不包含时间修饰与开发计划类内容。设计内容的任何变更均须同步至此文档。开发计划与排期见 [ROADMAP.md](ROADMAP.md)。

## 1. 产品概述

任务清单是一款**纯前端、单机优先**的任务管理工具：

- 无服务器、无登录、无网络请求；数据仅存于本机浏览器（localStorage），不上传任何服务器
- 部署于 GitHub Pages，免费 HTTPS，零运维
- 手机端可通过浏览器「安装应用」添加到桌面（独立窗口运行）
- 备份方式：导出/导入 JSON 文件到设备本地，导入支持合并同步

## 2. 整体架构与技术选型

**Vue 3 + Vite + TypeScript**

- 状态管理：Pinia（多列表、展开状态、外观设置全局化）
- i18n：vue-i18n，内置中/英双语，架构支持扩展更多语言
- 无 UI 组件库，手写组件 + CSS 变量主题（浅色/深色）
- 数据持久化：localStorage 单键 `tasklist:v1`，写入时防抖
- PWA：manifest + 简单 Service Worker（离线缓存），支持「安装到桌面」独立窗口
- 部署：GitHub Pages，`npm run build` 产物直接推送，零运维

### 2.1 数据模型

```ts
// localStorage 键：tasklist:v1
interface TaskData {
  version: 1             // 数据版本号，用于迁移（见 4.4）
  lists: List[]
  settings: { theme: 'light' | 'dark'; fontSize: number; lang: string; showDescription: boolean }
  ui: { sidebarCollapsed: boolean; expandedGroupIds: string[] } // 组合展开状态按 ID 记忆
}

interface List {
  id: string            // nanoid
  name: string
  description: string
  items: (Item | Group)[]
}

interface Item {        // 事项
  id: string
  name: string
  description: string
  date?: string         // yyyy-mm-dd，无日期则无时间
  time?: string         // hh:mm，有 time 必有 date
  done: boolean
  createdAt: number
}

interface Group {       // 组合（可递归嵌套，不限深度）
  id: string
  name: string
  description: string
  date?: string
  time?: string
  expanded: boolean     // 展开状态记忆
  items: (Item | Group)[] // 递归
  // 无 done 字段：完成状态与时间戳均由子项递归计算得出，不存库
}
```

## 3. 页面与组件设计

### 3.1 左侧栏

- 左侧栏展示「列表」列表
- 侧栏顶部有「收起/展开」按钮，页面尺寸较窄时为覆盖形式，点击遮罩或按钮收起
- 宽屏下同样可通过按钮收起/展开；收起后主区占据侧栏空间
- 列表项右侧有「编辑」按钮，切换全局编辑模式
- 列表最下方有「+ 新建列表」按钮
- 编辑模式下每个列表项最右侧展示「删除」按钮
- 双击列表名称可重命名（使用中央输入弹窗）
- 每个列表对应主区的一个任务清单
- 列表项颜色随其任务状态决定（红/蓝/绿）

### 3.2 右侧栏

- 右侧栏展示功能按钮
- 按钮文字单行显示（不允许按字符换行，避免窄宽下中文被拆成竖排）
- 页面尺寸较窄时（手机端）固定在底部
- 按钮依次为：
  1. `+ 事项`
  2. `+ 组合`
  3. `编辑`：切换全局编辑模式（开启后事项/组合右侧显示「编辑」「删除」等按钮）
  4. `设置`：打开设置弹窗（见 3.7）

### 3.3 主区

- 主区为清单内容，按状态分三组，自上而下：
  - 已逾期（红色）
  - 未完成（蓝色）
  - 已完成（绿色）
- 每组标题右侧有一条与状态同色的实线，延伸至主区右侧，视觉参考组合缩进竖线
- 组内按日期分隔展示小字日期（昨天/今天/明天/后天 或 x月x日）；无日期事项另以「无时间」分隔归类
- 列表无任何事项时，主区展示空态提示文案

### 3.4 事项

- 每个事项以一个相应颜色的横条圆角方框绘制
- 左侧有一空心圆心（未完成），或填充为绿色+白色勾（已完成）
- 黑色常规「事项名称」，灰色常规「 · 事项描述」（无描述时不展示）
- 最右侧展示日期（+时间，如果有）
- 点击左侧圆或整个事项可切换完成状态
- 已完成事项文字不加删除线、不变灰，仅靠左侧绿色圆勾区分
- 编辑状态下日期时间右侧展示「编辑」「删除」按钮
- 事项行用 flex-wrap 自适应：编辑模式按钮与标题、时间 chip 共处一行；位置足够时同行展示，不足时作为 flex 项目自然换到下一行，不允许按字符断行换行

### 3.5 组合

- 每个组合和事项一样绘制，颜色较深
- 左侧圆改为三角箭头表示展开/收起状态
- 黑色加粗「组合名称」，灰色常规「 · 组合描述」（无描述时不展示）
- 最右侧展示日期（+时间，如果有） · `X/Y 已完成`
  - 计数口径：统计组合内**全部递归子孙事项**（含直接子项与深层嵌套），X 为已完成数，Y 为总数；组合自身不计入
- 点击左侧箭头或整个组合可切换展开状态
- 展开后在下部缩进，缩进处绘制灰色竖线
- 缩进竖线距上方组合行底部与最下方子项底部各留 8px 间距，两端不贴边，避免嵌套组合竖线紧贴视觉
- 展开的组合整体作为一个拖放目标区域（子项区域也可触发放入该组合）
- 编辑状态下日期时间右侧展示「编辑」「+事项」「+组合」「删除」按钮
- 日期时间及右侧按钮皆为灰色，删除按钮在光标移上时变红
- 组合行用 flex-wrap 自适应：编辑模式按钮组与标题、时间 chip 共处一行；位置足够时同行展示，不足时作为 flex 项目自然换到下一行，不允许按字符断行换行

### 3.6 新建 / 编辑

所有新建/编辑均通过**中央弹窗**完成，不使用浏览器原生 `prompt` / `alert` / `confirm`。弹窗支持：

- `Esc` 取消
- `Enter` 确认（聚焦在输入框时）
- 点击遮罩关闭
- 必填项未填写时确认按钮禁用

#### 3.6.1 新建/编辑列表

- 输入框：列表名称（必填）
- 输入框：列表描述
- 取消 / 确认 按钮

#### 3.6.2 新建/编辑组合

- 输入框：组合名称（必填）
- 输入框：组合描述
- 取消 / 确认 按钮

#### 3.6.3 新建/编辑事项

- 输入框：事项名称（必填）
- 输入框：事项描述
- 开关：日期
  - 开启后展示日期选择
- 开关：时间
  - 勾选时间时自动勾选日期
  - 关闭日期时同时关闭时间
  - 开启后展示时间选择
- 日期与时间选择器：
  - 输入框本体使用原生 `<input type="date">` / `<input type="time">`，随 `color-scheme` 适配深浅色
  - 弹出的选择面板（日历、时间滚轮）为浏览器进程内 UI，无法通过 CSS/JS 样式化，保持原生外观
  - 自绘弹层为可选演进方向：日期选择器以日历弹层形式呈现，复用设计 token（颜色/圆角/阴影），支持年/月切换、今天高亮、点击选择；时间选择器以下拉列表或步进形式呈现。演进排期见 ROADMAP.md

### 3.7 设置

- 开关：显示描述
- 切换：外观（浅色模式 / 深色模式）
- 拖条：字号大小（即时全局生效，所有文本跟随缩放）
- 下拉框：语言
- 按钮：导入数据
- 按钮：导出数据
- 按钮：关于

### 3.8 提示与确认弹窗

- **提示弹窗**：纯信息展示，如导入成功、导入失败、数据损坏恢复提示、关于
- **确认弹窗**：删除、导入覆盖前二次确认
- **输入弹窗**：侧栏列表重命名、未来可能扩展的输入场景

## 4. 逻辑与交互

### 4.1 状态与时间戳

- 事项时间戳：
  - 有日期+时间 → `Date(date + 'T' + time)` 对应的时间戳
  - 有日期无时间 → 当日 00:00 的时间戳
  - 无日期 → 正无穷（组内排最后）
- 组合时间戳（递归计算）：
  - 若组合内存在已逾期或未完成事项，则为其中最小时间戳（无时间的仍为正无穷）
  - 若组合内事项全部已完成，则为已完成事项中除去无时间后的最大值
  - 若组合内无事项，则为正无穷
- 组合完成状态（递归计算，不可手动标记）：
  - 若组合内存在已逾期事项，则为已逾期
  - 若组合内事项全部已完成，则为已完成
  - 其他情况，则为未完成
- 已逾期判定：未完成且时间戳 < 当前时刻（有日期才可能逾期）

### 4.2 排序与分组

- 主区分三组，自上而下：已逾期 → 未完成 → 已完成
- 组内按时间戳升序（最早的在上），同一时间戳按名称排序；无时间戳的排组内最后
- 组间以日期分隔小字展示
- 组标题颜色与状态色同步（红/蓝/绿）
- 各列表在左侧栏的颜色同样随其状态判定

### 4.3 颜色映射

- 已逾期 = 红色；未完成 = 蓝色；已完成 = 绿色
- 组合颜色比同级事项深一档
- 分组小字与状态色同步；日期时间及右侧按钮为灰色，删除按钮在光标移上时变红

### 4.4 拖拽

- 位移阈值判定：位移 > 8px 进入拖拽，否则视为点击
- 进入拖拽后跟随半透明预览，预览样式跟随当前主题
- 拖拽期间禁止文本选择，释放后清除所有拖拽状态，不残留灰框
- 拖放到展开的组合上：整个组合区域（含子项区域）都可触发，放入该组合子层
- 拖放到事项上：默认移动位置；**按住 Ctrl 时**才与目标事项合并成新组合，组合名称取「A · B」
- 拖放到空白处：回到当前列表根层
- 放开时未落入有效目标（自身、自己的后代、无效 ID）则还原原位

### 4.5 状态刷新与持久化

- 逾期状态自动刷新：运行期间每 60 秒重算一次；页面从后台切回前台（visibilitychange）时立即重算
- 侧栏折叠 / 主题 / 字号 / 语言 / 组合展开状态均持久化记忆
- 导入合并：同 ID 覆盖本地，其余保留；导入前弹窗确认
- 导入校验：JSON 结构或字段不合法时弹窗提示并拒绝导入，不破坏本地数据
- 数据损坏与迁移：读取 localStorage 解析失败或数据版本不符时，先备份到 `tasklist:backup`，再重置为默认空数据并弹窗提示
- 跨标签页同步：监听 `storage` 事件，其他标签页写入时本页自动刷新内存数据

### 4.6 欢迎列表

- 启动时若左侧栏列表为空，则自动创建默认欢迎列表
- 默认内容放在 `src/data/welcome.json`，可直接编辑；`date` 支持相对标记 `today` / `tomorrow` / `yesterday` / `today+N` / `today-N`，首次创建时转换为绝对日期
- 各种名称必填，描述选填
